- name: Create Config file
  ansible.builtin.template:
    src: folder.xml
    dest: /tmp/job.xml

- name: Create
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/job.xml') }}"
    name: "{{item.dir}}"
    url: http://172.31.13.219:8080
    user: admin
    password: "{{jenkins_password}}"

- name: Create Config file
  ansible.builtin.template:
    src: "{{TEMPLATE}}"
    dest: /tmp/job.xml

- name: Create
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/job.xml') }}"
    name: "{{item.dir}}/{{item.name}}"
    url: http://172.31.13.219:8080
    user: admin
    password: "{{jenkins_password}}"